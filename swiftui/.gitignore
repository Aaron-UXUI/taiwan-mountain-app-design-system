.build/
.swiftpm/
*.xcuserdatad/
DerivedData/

# PreviewApp is a local-only visual QA helper (see PreviewApp/README.md).
# The .xcodeproj and GeneratedSources are regenerated by generate-pbxproj.mjs
# from the real Sources/ tree, so they aren't tracked — anyone who wants to
# run it locally runs the generator first.
PreviewApp/PreviewApp.xcodeproj/
PreviewApp/GeneratedSources/
